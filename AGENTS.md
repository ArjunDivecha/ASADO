## OpenWiki

This repository has documentation located in the /openwiki directory.

Start here:
- [OpenWiki quickstart](openwiki/quickstart.md)

OpenWiki includes repository overview, architecture notes, workflows, domain concepts, operations, integrations, testing guidance, and source maps.

When working in this repository, read the OpenWiki quickstart first, then follow its links to the relevant architecture, workflow, domain, operation, and testing notes.

## Learned User Preferences

- Keep ASADO code, data outputs, and pipeline artifacts under the ASADO workspace root unless the user explicitly directs otherwise.
- Design collection and ETL so it can run on a recurring schedule (for example monthly) and refresh the master database.
- When the user attaches an implementation plan: do not edit the plan file; use existing to-dos without recreating them.
- When building integrations, list required third-party API keys up front if the user wants to provision them in parallel.
- When an API key can't be obtained (e.g., email verification broken), prefer alternative free data sources over blocking on registration.
- For Bloomberg Terminal ingestion, use the user's OpusBloomberg pathway (Mac to Parallels to Terminal) and that project's configured Python environment; do not substitute a different Bloomberg connectivity approach.
- Exclude `.cursor/hooks/state/` (for example `continual-learning-index.json`) from git commits unless the user explicitly asks to version those files.
- For long autonomous research loops (e.g., LoopPilot runs), configure and run without pausing to ask questions, and send Telegram status updates so progress is visible.
- For work already approved in a PRD, build/test/verify autonomously (including overnight) without pausing for approval on simple decisions — only stop for genuinely undecidable choices — and leave a dated report in `docs/` (MORNING_REPORT_*/EOD_REPORT_*) with file:// links to artifacts.
- The warehouse and Neo4j graph exist to surface non-obvious connections to country returns and to suggest short- and long-term trade ideas; analyses should tie explanatory data back to return surfaces.

## Experimentation Rules (sandboxing vs the nightly pipeline)

ASADO is a base for experimentation, but the nightly pipeline (daily_update -> loop job -> briefs) and the Fable Daily Trading production system (`A Complete/Fable Daily Trading`, reads the loop DB read-only at 12:20 PT) must never be disturbed. Binding rules for every session; full detail in `experiments/README.md`:

1. **Main checkout = production.** launchd runs whatever is checked out here — never switch branches in this tree for an experiment. Pipeline-code experiments use a git worktree (`git worktree add "../ASADO-exp-<name>" -b exp/<name>`); merge back only what earns it.
2. **Snapshot, don't share.** Never hold a connection to `Data/asado.duckdb` or `Data/loop/asado_loop.duckdb` — even idle READ-ONLY connections block the nightly writers (one-writer-or-many-readers; caused the Jul 2-3 pipeline failures). Freeze inputs first with `scripts/snapshot_for_experiment.py --db loop --tables <t1,t2> --name <exp>` and work off the parquet. Direct live-DB access = open, query, close within seconds.
3. **Own your output space.** Keepable code/results: `experiments/<YYYY_MM>_<name>/` (committed, short README each). Scratch + snapshots: `Data/work/experiments/<name>/` (gitignored). Experiments NEVER write into `Data/processed/`, `Data/loop/`, the shared `Data/work/{t2,gdelt,econ,loop,...}` pipeline dirs, `config/`, or `ledgers/`.
4. **Verdicts through the front door.** Exploration is free; any "this signal works" claim must register a harness trial charged to a family in `config/family_registry.yaml`. Never `sweep_signals.py --force` for re-measurement.
5. **Isolate environments.** New packages go in a per-experiment venv (`uv venv` inside the experiment dir), never into `ASADO/venv` (the nightly depends on it). Avoid long-held resources 06:00-08:30 PT.

## Learned Workspace Facts

- ASADO workspace root: `/Users/arjundivecha/Dropbox/AAA Backup/A Working/ASADO`; 34-country T2 universe defined in `config/country_mapping.json`; git remote https://github.com/ArjunDivecha/ASADO.
- JST Macrohistory (1870-2020) is an ISOLATED long-cycle calibration corpus, NOT a factor feed: table `jst_macrohistory` only, never unioned into `unified_panel`/`feature_panel`, never forward-filled to monthly (same rule as the deprecated `wb_commodity_factor_panel`). Ingest `scripts/collect_jst_macrohistory.py` (static, not nightly); calibrate `scripts/calibrate_jst_bearbottom.py`; live read-only accessor `regime/calib/jst_calib.py`; surfaced as context in the dislocation brief. `Data/raw/jst/` + `Data/processed/jst_macrohistory_panel.parquet` are gitignored — regenerate by running the collector. See `docs/JST_MACROHISTORY_CALIBRATION.md`.
- T2/GDELT inputs are now internalized (no external Fuzzy/Excel steps): `scripts/setup_duckdb.py` reads `Data/work/t2/Normalized_T2_MasterCSV.csv`, `Data/work/t2/T2 Master.xlsx`, and `Data/work/gdelt/GDELT_Factors_MasterCSV.csv`; all pipeline intermediates live under `Data/work/{t2,gdelt,econ,t2_daily,gdelt_daily}`.
- Phase 1 external data specs, paths, and indicator definitions live in `CLAUDE_CODE_BRIEF.md` and `PRD_Phase1_Program1_External_Data.md` in this repo.
- Credentials: FRED and EIA keys are expected in the project `.env` file; Kalshi API credentials (key id + RSA private key) live in `/Users/arjundivecha/Dropbox/AAA Backup/.env.txt` for optional `build_predmkt_panel.py` auth.
- `scripts/` pipeline: collectors `collect_external.py`, `collect_extended.py`, `collect_imf.py`, `collect_bilateral.py`, `collect_bloomberg.py` (Bloomberg via `/Users/arjundivecha/Dropbox/AAA Backup/A Working/OpusBloomberg` and its venv); `setup_duckdb.py`, `setup_neo4j.py`, `build_embeddings.py`, `db_bridge.py`, and monthly orchestrator `monthly_update.py` (run from repo root; collectors use source-level merge, 24h cache, timestamped backups).
- IMF data uses the SDMX 3.0 API at `api.imf.org` with no API key required. The legacy `dataservices.imf.org` endpoint is defunct.
- DuckDB at `Data/asado.duckdb`; `unified_panel` view joins 7 tables (`t2_master`, `t2_raw`, `external_factors`, `extended_factors`, `gdelt_panel`, `imf_factors`, `bloomberg_factors`). Rebuilt size is on the order of ~4 GB (monthly plus daily extension). Daily tables include `t2_factors_daily`, `t2_levels_daily`, `gdelt_factors_daily`, `gdelt_raw_daily`, `factor_returns_daily`, `variable_meta`, `daily_calendar`.
- Daily pipelines: `scripts/daily_update.py` runs the daily metronome (Bloomberg-direct T2 pull + GDELT factors/returns, ~12-15 min; flags `--skip-bloomberg`, `--t2-only`) writing to `Data/work/t2_daily/` and `Data/work/gdelt_daily/`; `scripts/build_daily_panels.py` builds the DuckDB daily extension (orchestrator uses `--rebuild --no-backup`). MCP tools `event_window` and `daily_factor_series` in `asado_mcp_server.py` expose daily data.
- Market-implied stress layer (2026-06-11): `scripts/loop/collect_market_implied_bbg.py` (OpusBloomberg env, 65 tickers, ~65 hits/night) + `scripts/loop/load_market_implied.py` -> loop-DB tables `market_implied_daily` (FX 1M implied vol + sign-normalized 25D risk reversals for 24 T2 pairs/29 countries, VIX/VIX3M/MOVE/HY-IG OAS/DXY/BBDXY, CL/CO/HG/GC/NG 1st+2nd generics; 2006+) and `market_implied_signals` (252d z-scores, VIX term ratio, curve-shape %). Both steps in `loop_daily_job.py`; brief has a "Market-implied stress" section, and detector D10 (A10, v1 2026-06-11, in `build_dislocations.py`) fires on FX-options-vs-equity conflicts: `fx_options_stress_unpriced_by_equity` (RR z >= 2 or vol z >= 2.5, |eq21 z| <= 0.75) and `equity_stress_unconfirmed_by_fx_options` (eq5 z <= -1.5, RR+vol z <= 0.5); HKD/SAR pegs included with a peg note, US complex + Denmark/Vietnam structurally skipped. RR convention: positive = options premium on local-currency depreciation. No FX surface for Denmark/Vietnam; US complex is the USD numeraire (DXY rows). Quota log: `Data/work/loop/bbg_quota_log.csv`.
- Stage 2 prediction markets: built by `scripts/build_predmkt_panel.py` from curated registry `config/predmkt_curated.yaml`; DuckDB holds `predmkt_daily`, `predmkt_market_meta`, `predmkt_outcome_meta`, `predmkt_country_spillover`, `predmkt_resolutions`, and `predmkt_signals_daily`. MCP exposes `predmkt_snapshot`, `country_signal_now`, and `event_market_set`. For country composites, `predmkt_country_risk_composite` > 0 means implied downside risk; `predmkt_country_opportunity_composite` > 0 means implied upside.
- Neo4j at `bolt://localhost:7687` — start with `brew services start neo4j` before graph operations. Factor nodes carry daily performance stats from `factor_returns_daily` (`daily_sharpe_252d`, `daily_vol_252d`, `daily_max_drawdown_252d`, `daily_cum_return_252d`), set in `setup_neo4j.py`. The `is_optimizer_selected` flag and the "8 live factors" concept were removed 2026-06-10 — they were stale artifacts of the retired Fuzzy Daily project (static `T2_Optimizer_Top.xlsx`), not a live strategy. Do not reintroduce them.
- Bloomberg collector (`collect_bloomberg.py`): multi-phase bonds, CDS, OIS, WIRP, ECFC/EHGD consensus, PMI, M2, derived signals; `BBG` supports `ref()`, `ref_batch()`, `hist()`, `bds()`. Ticker quirks: ECFC try `ECGD[CC]` then `EHGD[CC]Y` (Switzerland `ECGDSW`, not `ECGDCH`); PMI `MPMI[CC]MA/SA` with Japan `JA` not `JN`; M2 tries multiple candidates per country.
- Regime research lives in `regime/` (Regime Conditioning Value Test: PRD, `src/`, `results/`, report `regime2.md`) and `regime_loop/` (LoopPilot iteration outputs). LoopPilot is the user's autonomous research-loop framework at `/Users/arjundivecha/Dropbox/AAA Backup/A Working/Loop Pilot` (agent proposes changes, deterministic runner snapshots/evals/promotes, Telegram oversight).
- Bloomberg monthly ticker map fully re-audited 2026-06-11 (`collect_bloomberg.py`): Saudi GSAB* family (5Y/10Y/30Y) was South Africa's data; many EM 2Y/5Y/10Y `* Index` generics dead — replaced with `GT[CCY]*Y Govt` generics which carry PRICE in `PX_LAST` (yield via `YLD_YTM_MID`; see `_bond_field` rule). 5 dead EUR-era CDS replaced with ISDA-2014 `... CDS USD SR 5Y D14 Corp` names. Sovereign rating fields (RTG_*) only exist on real bond securities, never on generic yield indices — ratings pull from `GT[CCY]10Y Govt` and are stored as numeric 21-point scores (`RATING_SCALE`, AAA=21). `merge_panels` drops non-T2 countries loudly (MSCI weights can emit Austria/Greece/etc.).
- Prediction-market registry expanded to ~150 entries on 2026-06-11: `scripts/loop/discover_predmkt_candidates.py` sweeps Kalshi macro series + Polymarket top-volume actives into draft YAML (`Data/work/loop/predmkt_candidates_draft.yaml`) for agent curation. Sign convention: positive elasticity = YES hurts that country; de-escalation markets (peace/ceasefire/normal/deal) carry flipped template signs; oil LOW strikes use category `oil_crash` with flipped oil template. Resolved markets stay in the registry for resolution tracking (validator warnings on them are expected).
- `validate_returns_first.py` check 7 (GDELT alias) treats both-sides-NULL forward returns (open month) as upholding the alias; only one-sided NULLs or unequal values are violations (v1.1, 2026-06-11).
- Foreign investor flows (loop table `foreign_flows_daily`, parquet `Data/work/loop/foreign_flows.parquet`): India via NSDL FPI scraper `scripts/loop/collect_foreign_flows.py` (reporting-date = trade date + 1, 2017+); Korea/Taiwan/Thailand/Philippines/Indonesia via Bloomberg `scripts/loop/collect_foreign_flows_bbg.py` (trade-dated, 2005+, OpusBloomberg env). Verified Bloomberg tickers: KPCPNTFR+KQCPNTFR (Korea, KRW mn), TAFINET/TAFIBUY/TAFISELL (Taiwan, TWD mn), THIVNET$ (Thailand, USD mn), VUPHBNET (Philippines, USD mn), JASXFIBA−JASXFISA (Indonesia, IDR mn), FIINNET$ (India cross-check, USD mn). KRX/TWSE/SET/IDX websites geo-block this network — use Bloomberg, not scraping, for those markets.
- Sovereign daily CDS/10Y (loop table `sovereign_daily`, parquet `Data/work/loop/sovereign_daily.parquet`): `scripts/loop/collect_sovereign_daily_bbg.py` (OpusBloomberg env, parquet only) + `scripts/loop/load_sovereign_daily.py` (venv, table rebuild). 20 CDS series 2005+ (`SOV_CDS_5Y_BP`), 32/34 10Y (`SOV_10Y_YIELD_PCT`; no Hong Kong/Vietnam). Ticker corrections vs the monthly collector's map: Chile 10Y = `CLGB10Y Index` (CHILE10 invalid); France/Italy/Poland/Spain/South Africa CDS need ISDA-2014 names (`FRANCE/ITALY/POLAND/SPAIN CDS USD SR 5Y D14 Corp`, `REPSOU ... D14`; history from 2014-09); Malaysia/Philippines/Turkey/Saudi 10Y via `GT{MYR,PHP,TRY,SAR}10Y Govt` pulling `YLD_YTM_MID` (PX_LAST is price on Govt generics); Taiwan 10Y = `TPGBTW10 Index`. BUG found in `collect_bloomberg.py` COUNTRY_TICKERS: `GSAB10YR Index` is SOUTH AFRICA's 10Y but was assigned to Saudi Arabia (FIXED in the 2026-06-11 monthly-map re-audit). Also t2_levels_daily "10Yr Bond" for Brazil is wrong (~2.85 vs real ~14.8) — D4/valuation use sovereign_daily instead.
- Valuation block (loop table `valuation_monthly`, parquet `Data/work/loop/valuation_monthly.parquet`): `scripts/loop/build_valuation_block.py`, month-end CAPE/PB/DY/EY/Trailing-PE + ERP (= EY − (10Y − CPI YoY), 32 countries) + `_PCTILE_10Y` percentiles (100 = richest own-decade reading; DY/EY/ERP inverted so rich is always 100). No new Bloomberg pull — reads t2_levels_daily + sovereign_daily.
- WEO surprise surface (loop tables `weo_vintages` + `weo_revisions`, parquet `Data/work/loop/weo_vintages.parquet`): `scripts/loop/collect_weo_vintages.py`. 36 vintages 2008-04..2026-04: archives via DBnomics (`api.db.nomics.world`, IMF provider, `WEO:YYYY-MM` datasets — imf.org bulk downloads are Akamai-blocked for scripts; DBnomics lacks 2011-10), 2025-10 + current vintage via IMF SDMX 3.0 (`IMF.RES/WEO_2025_OCT_VINTAGE`, `IMF.RES/WEO`). Variables `WEO_GDP_GROWTH_PCT`/`WEO_CPI_INFLATION_PCT`, target years vintage_year−1..+5. Detector D3 (revision momentum) is live off `weo_revisions`, z-scored vs each country's own 2008+ revision history, 120-day live-news window per vintage.
- ToT trade shares v2 (D1 v1, 2026-06-11): `build_tot_shares.py` sources ALL 34 countries from the KEYLESS UN Comtrade public preview endpoint (`comtradeapi.un.org/public/v1/preview/C/A/HS`, HS 2-digit chapters batched per reporter), 6 categories incl. precious (HS 71) + fertilizer (HS 31) that WDI sections missed (South Africa precious net +18% of trade). Gotchas: preview truncates at 500 rows — MUST pass server-side `motCode=0&partner2Code=0&customsCode=C00` or big traders (DEU/FRA/GBR) lose their TOTAL rows; reporter codes are non-M49 in places (France 251, India 699, Switzerland 757, USA 842, Taiwan 490); endpoint 429s under bursts (65s backoff + 7-day freshness gate so the nightly job doesn't hammer it). D1 basket re-validated against 3 known episodes (Saudi/oil 2020 z=−3.3, Chile/copper 2021 z=+2.0, Chile/copper 2024 z=+1.5) via `scripts/loop/validate_d1_episodes.py` — PRD Phase 3 acceptance PASS.
- ETF positioning (loop tables `etf_flows` + `etf_flow_signals`, parquet `Data/work/loop/etf_flows.parquet`): `scripts/loop/collect_etf_flows_bbg.py` (OpusBloomberg env; EQY_SH_OUT/FUND_NET_ASSET_VAL/FUND_TOTAL_ASSETS daily, 34 ETFs from `config/etf_t2_map.json`, 2010+) + `scripts/loop/load_etf_flows.py` (derives ETF_FLOW_USD_MN = dShares x NAV with a 20% split guard, 21d sum, %AUM, z vs trailing year). Brief shows |z|>=2 extremes. This is PRD P11's share-count flow layer and the de facto Brazil flow proxy — B3 has NO live Bloomberg foreign-flow series (monthly BZSIFITO died 2021-08, BM&F futures positioning BMFCIB*F stale since 2025-10).
- ECFC consensus surface (loop tables `consensus_daily` + `consensus_revisions`, parquet `Data/work/loop/consensus_daily.parquet`): `scripts/loop/collect_consensus_bbg.py` (OpusBloomberg env) + `scripts/loop/load_consensus.py`. KEY DISCOVERY: year-specific ECFC tickers `ECGD{ISO2} {YY} Index` / `ECPI{ISO2} {YY} Index` store the FULL daily revision path per target year (no snapshot accumulation needed) — 1,216 series, 921K rows, 2007-10+. This family uses ISO-2 codes (CN=China, CH=Switzerland, BR/JP/KR/TR), NOT the legacy rolling-ticker codes in `collect_bloomberg.py` (BZ/JN/KO/TU/SW; there `ECGDCH` would be Switzerland's year-ticker but China's rolling ticker — never mix the two families).
- GDELT evidence layer (loop table `gdelt_articles_recent` 14d retention; permanent packs `Data/loop/evidence_packs/{date}/{country}.json`): `scripts/loop/build_evidence_packs.py` freezes headlines for countries whose dislocations FIRED (status new/intensifying, D8 excluded, max 12 pulls/night by |severity|) + event_log entries within 2 days. GDELT DOC API throttles hard and its cooldown resets on every probe: never loop retries; the script aborts remaining pulls on the first rate-limit and exits non-zero.
- ETF short interest rides the ETF-flows collector (`SHORT_INT`/`SHORT_INT_RATIO`, semi-monthly FINRA prints via Bloomberg): signals `ETF_SHORT_PCT_SHOUT` + `ETF_SHORT_PCT_Z` (z vs ~2y, 48 prints/yr), 32/34 countries (INDA none, EDEN no ratio). COT: `scripts/loop/collect_cot.py` (CFTC Socrata `publicreporting.cftc.gov/resource/6dca-aqww.json`, keyless, 12 commodity futures keyed by stable contract market codes, 2006+) → loop tables `cot_weekly` + `cot_signals` (net %OI + 52w z). Both surface in the brief (SI |z|≥2, COT |z|≥1.5).
- Calibration report (PRD 6.3, Phase 4 machinery): `scripts/loop/calibration_report.py` regenerates `Data/loop/calibration/calibration_YYYY_MM.{md,xlsx}` nightly from folded ledgers — Brier/hit-rate by archetype/horizon/direction/regime-at-open/author + calibration curve; stamps PARTIAL until ≥10 closed theses. Runs as the last loop-job step (after fold_ledgers). Nightly loop job = 27 steps as of 2026-06-12.
- Bloomberg skill-mining batch 2026-06-12 (six layers, `docs/BBG_SKILL_ENHANCEMENTS_2026_06_12.md`; loop job now 27 steps): (a) market-implied collector v1.2 adds 1W/3M ATM vol, 25Δ butterflies (`[PAIR]25B1M`), and 3M forward/NDF carry — `[ROOT]3M Curncy` `PX_LAST` is the OUTRIGHT forward (not points), NDF roots BCN/KWN/NTN/IRN/IHN/MRN/PPN/CHN for BRL/KRW/TWD/INR/IDR/MYR/PHP/CLP, carry sign-normalized positive = local rates above USD; signals add `FX_BF25_Z252`, `FX_VOL_TERM_PCT/_Z252` (1W−3M; positive = event priced NOW), `FX_CARRY_3M_PCT/_Z252`. (b) sovereign daily v1.1 adds 1Y CDS (tenor substitution on the 5Y names; Poland/Vietnam have no 1Y quote) and 2Y yields (27 countries; Brazil's `GEBR2Y Index` is dead → `GTBRL2Y Govt`+`YLD_YTM_MID`, fixed in BOTH daily and monthly collectors) → new table `sovereign_signals` (`SOV_CDS_SLOPE_BP` 5Y−1Y, inversion = imminent-distress pricing; `SOV_2S10S_PCT`; both with shifted 252d z).
- BQL sovereign ratings (first BQL collector, `scripts/loop/collect_sov_ratings_bql.py` + `load_sov_ratings.py`): raw blpapi on `//blp/bqlsvc` with `clientContext.appName="EXCEL"` unlock; the BQL response arrives as a message whose messageType IS `result` — parse via `json.loads(msg.asElement().getValueAsString())`. KEY: `rating()` on generics returns null — use `get(rating(source=SP|MOODY|FITCH, dates=range(-20Y,0D,frq=M))) for(issuerof('<anchor>'))`; anchor = the country's 5Y CDS contract where one exists (full FC issuer history; MEX/ZAF/TUR local GT generics resolve to NO entity), else `GT[CCY]10Y Govt`. 33/34 countries (no Hong Kong anchor), monthly 2015+ (Moody's 2017+), 21-pt scale → loop tables `sov_ratings_monthly` + dated `sov_rating_changes` (128 events, verified vs public record). BQL dates are tz-aware — `tz_localize(None)` before to_period.
- Economic surprise layer (`scripts/loop/collect_eco_surprise_bbg.py` + `load_eco_surprise.py`): BDH of `ACTUAL_RELEASE` + `BN_SURVEY_MEDIAN` on ECO release tickers (CPI YoY 31 countries, unemployment 22, GDP 17, Markit PMI 20; surveys ~2015+). The findatapy econ grid's CPI/UNEMP/PMI tickers mostly work but its GDP tickers are nearly all dead — GDP map rebuilt by live Terminal probe; no survey-carrying GDP ticker for CA/CL/DK/IN/IT/NL/PH/CH/TW/TH/TR, no survey unemployment for DK/FR/ES/TH/TR; Poland unemployment = `POUER Index`. Loop tables `eco_surprise_monthly` + `eco_surprise_signals` (`ECO_*_SURPRISE_Z` per print scaled by trailing 60m own-surprise std, composites `ECO_GROWTH_SURPRISE_Z` = mean(GDP, PMI, −UNEMP) and `ECO_INFL_SURPRISE_Z` = CPI). Brief gained a "Sovereign curves, ratings & macro surprises" section.
- Running list of user-side fixes lives in `docs/USER_FIX_LIST.md` — append new warehouse/upstream bugs there (agent does not fix monthly-collector or T2-feed bugs without explicit approval).
- `setup_duckdb.py` DELETES and recreates `Data/asado.duckdb` on every rebuild — never create persistent tables directly in the main DB. Loop surfaces live in a separate `Data/loop/asado_loop.duckdb` plus parquet-first sources (under `Data/loop/` and `Data/work/loop/`) so they survive rebuilds; the predmkt daily job archives/restores parquets for the same reason.
- LOOKAHEAD TRAP: T2 `NMRet` variables (`1MRet`/`3MRet`/`6MRet`/`12MRet` + daily analogs) are FORWARD returns anchored at the window start (optimizer targets), not trailing momentum — hard-blacklisted as harness signals; trailing momentum uses `12-1MTR_CS`/`120DTR`-style variables. `country_returns_monthly` labels = the month the (decimal) return was realized in.
- Alpha-Hunting Loop (`PRD_Alpha_Hunting_Loop.md`): event-sourced append-only JSONL ledgers in `ledgers/` (hypotheses `H_YYYYMMDD_NNN`, theses) folded into the loop DB; skeptic harness `scripts/harness/evaluate_signal.py` (PIT canary tests in `tests/loop/`, NW-t/deflated-Sharpe gates, WATCH/WEAK/DEAD verdicts); dislocation engine `scripts/loop/build_dislocations.py` (detectors D1–D9, rows labeled by data as-of date, idempotent delete-and-reinsert per date) writes the nightly brief `Data/dislocations/brief_YYYY_MM_DD.md` incl. forward calendar (`config/forward_calendar_seed.yaml` + `scripts/loop/build_forward_calendar.py`). Orchestrator `scripts/loop/loop_daily_job.py` runs as the final stage of `daily_update.py`; launchd: predmkt 06:30, standalone loop safety-net 11:30.
- Variable registry (semantic layer): seed `config/variable_registry_seed.yaml` (builder `scripts/build_variable_registry.py` only appends skeletons, never edits existing rows), Tier-A semantics `config/tier_a_semantics.yaml` + `scripts/apply_tier_a_semantics.py`, CI guard `scripts/qa/validate_variable_registry.py` (15 checks), rendered `docs/VARIABLE_DICTIONARY.md`; tables `variable_registry`/`variable_registry_facts`/`variable_registry_full` in asado.duckdb, wired into `monthly_update.py`. Sign gotcha: t2 `_CS/_TS` variants are sign-flipped during construction (`t2_normalize.py`) but econ `_CS/_TS` from `build_normalized_panel.py` inherit the raw sign. Live universe ≈1,434 variables; `demographics_dip` extends to 2100-12 so unclamped "latest value" queries read projections.
- News-repo bridge: `/Users/arjundivecha/Dropbox/AAA Backup/A Working/News` `report.db` (SQLite) keeps only ONE snapshot date (overwritten daily), so the bridge accumulates it into loop tables `portfolio_holdings_daily` + `etf_prices_daily` (archive `Data/loop/news_bridge_archive/`). Morning routine when IBKR/Schwab/Bloomberg are up: News pipeline → `daily_update.py` → loop job.
- Analysis tools (2026-06-12): batch sweep runner `scripts/harness/sweep_signals.py` (YAML specs in `config/sweeps/`, pre-registers every entry as a ledger trial with mandatory mechanism text, summaries to `Data/loop/harness_runs/sweeps/`) and event-study engine `scripts/loop/event_study.py` (CARs on T2 country returns, presets rating_downgrade/upgrade, cds_inversion, growth_hot/cold, inflation_hot, dislocation, event_log, or `--events-sql`; outputs to `Data/loop/event_studies/`). Harness `evaluate_signal.py` scales its coverage gate (`max(10, ceil(0.8*n))`) and top-N (`max(3, n//3)`) when a sub-universe is declared — declare honest universes (FX options surface = 26 stable countries, daily 2s10s = 20, growth-surprise composite = 14; 1Y CDS too thin to cross-sectionally rank, event-study only). First family `bbg_skill_2026_06` = 15 trials: 14 DEAD/insufficient, 1 WEAK (`ECO_INFL_SURPRISE_Z` flipped: hot inflation prints → outperformance, nw_t≈3.0, DSR still negative). Event-study finding: rating downgrades → −0.7% CAR at +5d (t≈−2.0, 73 events).
- Systematic harness pass 2026-06-12 (18 trials over previously untested loop families; specs in `config/sweeps/`): graph spillover family (`family_key=graph_trade_gap`, now 7 trials) is the standout — `GRAPH_BANK_NBR_RET_GAP_21D` ic=0.034 nw_t=5.7, `GRAPH_TWOHOP_TRADE_GAP_21D` nw_t=4.5, both WEAK (DSR negative; edges are current-weight not PIT — vintage-edge robustness required before promotion). `CONS_CPI_REV3M_12M` WEAK nw_t=2.3 from 2013 (reflation direction, consistent with flipped inflation surprise H_015). `ETF_FLOW_21D_Z` significant AGAINST registered direction (contrarian, nw_t=−2.2; unclaimed without a new flipped trial). Valuation percentiles, short interest, GDP revisions, FX vol level: DEAD. New `consensus_signals` table (`load_consensus.py` v1.1): `CONS_GDP_REV3M_12M`/`CONS_CPI_REV3M_12M` = fixed-12m-horizon blend of 3m ECFC revisions (w=(12-month)/12 on current target year), 34 countries 2008+, full coverage 2013+. Event studies: CDS curve inversion → −4.5% CAR at +63d (t=−2.4, 41 events); rating upgrades ALSO drift negative (−2.4% at 63d, n=45); growth surprises show no event drift; `dislocation` preset is INSUFFICIENT_EVENTS until detector history (begins 2026-06-09) accumulates a forward window.
- Git hygiene: heavy data dirs are gitignored (`T2 Daily Update/` ~3.4 GB, `Data/loop/`, `Data/vintages/`, `*.bak`); the JSONL ledgers and dislocation briefs ARE committed. Repo has a single `main` branch (feature branch squash-merged, old worktrees removed Jun 2026).
- Graph machine 2026-06-12 (nightly loop now 32 steps; README "The graph machine"): PIT edge vintages `scripts/loop/collect_pit_edges.py` (IMF IMTS annual trade 1999+ — the SDMX query returns ALL years, the monthly collector just kept the latest; BIS LBS quarterly 1999+ with `startPeriod`; holder = warehouse `bilateral_portfolio_matrix` imf_pip rows; publication lags trade/bank +4m, holder +9m; runs in monthly_update step 4b, NOT nightly) → `graph_edge_vintages`. Features: `build_graph_features_pit.py` (GRAPHP_* incl. Katz 3-hop α=0.5, PageRank hub-amplified gap, spectral k=4 trade-bloc gap), `build_similarity_features.py` (top-5 cosine fundamental twins from ~41 month-end `_CS` factors, categories valuation_fund/macro/rates/fx/commodity/risk/size/volatility — NEVER return/technical; some `_CS` values are ±inf, replace before norms, and guard `np.isfinite` because `NaN <= 0` is False), `build_leadlag_features.py` (lag-1 cross-corr ≥0.15 network, U.S. complex dominates as leader/timezone channel), `build_combiner.py` (walk-forward ridge; monthly DEAD — month-end sampling kills days-to-weeks components; daily on 6 market-derived survivors = strongest ledger signal IC 0.057 nw_t 10.7 u29 2006+, ceiling pending forward verification), `write_graph_discoveries.py` (Neo4j SIMILAR_TO/LEADS edges + Country.combiner_score/rank).
- PIT re-test verdict (family graph_trade_gap, 15 trials): the graph family SURVIVED point-in-time edges — pit twohop nw_t 4.4, Katz 4.25, trade-gap-21d 4.1, hub 4.0, bloc 3.2, bank gap ic 0.034 nw_t 4.5 on 17 stable-coverage countries 2008+ (PIT-vs-current feature corr 0.988). Lead-lag family: 5d gap ic 0.057 nw_t 8.5 on 16 structural followers (Asia+Europe; U.S. leads but is never led, so it can't be in follower universes). Fundamental twins ic 0.028 nw_t 5.6. ETF flow contrarian flip registered and confirmed WEAK (nw_t −2.2). Harness lesson: `infer_publication_lag` defaults monthly non-whitelisted sources to a 1-month lag — market-derived monthly tables (e.g. combiner scores) must set `publication_lag_months: 0` in the sweep spec or they get tested one month stale. Sweep dedupe hashes (table, variable, lag, direction, mechanism) but NOT start_date/universe — coverage re-specs need a changed name AND --force or a distinguishable spec. Conditioned event studies: downgrade drift is an EM phenomenon (EM −0.9%@5d t=−2.0, −2.8%@63d; DM flat); crisis-history edges are useless for conditioning (every country links to the same 9-11 global crises).
- JST corpus usage (2026-06-16; nightly loop now 33 steps): the JST tail tables are now consumed in two read-only places. (a) The daily brief's "Long-cycle tail context" section (`build_dislocations.py::_jst_tail_context_section`) labels each deep-drawdown country `DM` (one of the 13 JST-calibrated developed markets in `regime/calib/jst_calib.JST_DM_COUNTRIES` / `is_jst_dm()`) vs `EM-analogy` (every other T2 name — the DM distribution is a directional analogy, not a same-market estimate), DMs sorted first. (b) New step 33 `scripts/loop/build_jst_risk_report.py` (read-only, project venv) writes a dated tail-risk report to `Data/loop/risk_reports/jst_tail_risk_YYYY_MM_DD.{xlsx,pdf}` — all 34 countries' current drawdown → JST bucket → forward 1/3/5y REAL equity distribution (the p10 once-in-a-century downside), filename dated by DATA AS-OF (last returns date), not wall-clock. Caveats baked into the report: live drawdown is NOMINAL vs JST REAL; tail cells use overlapping annual windows (n_obs overstates independent N — clean count is the 65 banking-crisis onsets); pure context, must clear the harness before influencing any trade/severity. The 13 in-scope DMs are the source-of-truth `JST_TO_T2.values()` from `collect_jst_macrohistory.py`.
- Cost/holding-period model (harness v2.1, 2026-06-12): every daily run now emits `hold_period_grid` (same ranks re-costed at 1d/5d/21d tranched holds), `breakeven_cost_bps_ls` (one-way bps where mean net LS return = 0), and a 5 bps cost case; verdict gates unchanged (still net-25bps at the registered hold). All 29 verdicted daily hypotheses were re-measured IN PLACE by calling `evaluate_signal` with the existing hypothesis_id + stored run params (re-verdict events append, fold keeps latest, ZERO new registrations — `sweep_signals.py --force` would instead register NEW trials and inflate the family count; never use it for measurement upgrades). Quantified answer (`Data/loop/harness_runs/cost_model_summary_2026_06_12.xlsx`): nothing survives 25 bps one-way; at 10 bps four signals clear net LS Sharpe > 0.3 (daily ridge combiner breakeven 14.2 bps / net 0.99 @10bps / 2.15 @5bps, GRAPHP bank gap 13.1, SIM twins 63d 14.1, GRAPH bank 63d 13.7); at 5 bps twelve clear. Structural finding: top daily signals decay fast — gross Sharpe falls faster with hold length than turnover savings, so 1-day holds win NET for the strong family; 21d holds only rescue slow signals (SOV_2S10S breakeven 15.8). Graph-family breakevens cluster 8–14 bps: real but thin edges, futures/cheap-DM-ETF execution only.
- Ken French style-spanning (2026-06-17): `collect_ff_factors.py` ingests the FF 5 factors + momentum + RF for 8 FF regions (US, Developed, Developed_ex_US, North_America, Europe, Japan, Asia_Pacific_ex_Japan, Emerging) into the ISOLATED `ff_factors` table (region-keyed, monthly+daily, USD, values in PERCENT; 540K rows; US to 1926/63, regions to 1990, Emerging monthly-only to 1989 — French publishes NO daily Emerging file, both 404). Wired into `monthly_update.py` Stage 1 as "Program 5c" (`--skip-ff`), `setup_duckdb.load_ff_factors` (DROP+CREATE, idempotent, NOT in `create_unified_view`), auto-documented by the schema registry → `build_factor_reference.py`. GOTCHA — it is NOT broadcast to the 34 countries and NEVER enters `unified_panel`/`feature_panel` (same rule as JST / the deprecated `wb_commodity_factor_panel`; don't "fix" it by tiling). Country→region map is `config/ff_region_map.json` (written by the collector; Saudi Arabia/Vietnam are `confidence:"proxy"` — not in the FF EM universe, Emerging is least-bad). Momentum is stored as `WML` for every region (the US file's column is `Mom`, normalised on load). The parser keeps only rows whose first CSV token is 6 digits (monthly) / 8 digits (daily), which auto-drops French's trailing ANNUAL block + copyright footer; `-99.99`/`-999` → NaN (EM RMW/CMA are missing pre-~2000). USAGE: `scripts/harness/ff_spanning.py` regresses a return series on a regional model (`capm`/`ff3`/`carhart`/`ff5`/`ff5_mom`) → alpha + Newey–West HAC t (Bartlett kernel matching `evaluate_signal.nw_tstat`), betas, R². It connects DIRECTLY to DuckDB (NOT via `AsadoDB`, which requires Neo4j). Pass `subtract_rf=False` for a self-financing long-short P&L (default), `True` for a long-only total return; `ff_factor_series`/`ff_region_of` added to `db_bridge`. Sanity check: ASADO `U.S.` return spans FF US market β≈0.99 R²≈0.996 α≈0; `span_country_returns` uses `country_returns_monthly` (total return → subtract_rf=True). Values are stored in PERCENT — the spanning/bridge `units="decimal"` path divides by 100, don't double-convert.
- Triptych Prediction Prior Layer (2026-07-02): the Triptych visual tool's analytics kernel is ported natively into ASADO — `scripts/loop/triptych_kernel.py` (line-verified vs the tool's `app/assets/core.js`: Welford expanding z with POPULATION variance, self-EXCLUDED cross-country z, linear-interp decile thresholds, PIT bucketing = insert-own-signal-then-assign with 36-obs warm-up, overlap-adjusted t (n_eff=n/horizon), Spearman IC on average-ties ranks). Nightly step `build_triptych_scan.py` (after render_dislocation_brief) sweeps all t2_raw factors + the warehouse variables declared in `config/triptych_scan.yaml` (extend the scan by adding a line there) × countries × {raw,history_z,cross_var_pct} × {absolute,relative} × 6 horizons in BOTH threshold modes → loop tables `triptych_scan`/`triptych_review_queue` + view `triptych_priors` (~10 s all-cores; 165K rows). HARD RULES: full-sample rows are descriptive-only (confidence hard-zeroed in `finalize()`, filtered again by every consumer); the Triptych app's own `slope_r2_scan.py` xlsx is full-sample only — never ingest it as a prior; `triptych_url` deep links exist only for t2_raw factors (the tool serves the T2 workbook); global-broadcast factors auto-skip cross_var_pct (degenerate cross-section). Surfaces: cockpit "Triptych" desk tab + country-letter "Conditional history" (epistemic tag PRIOR — triage input, never evidence), Fable packet block `triptych_priors` (+"triptych" in its surfaces enum), MCP `triptych_link`/`triptych_prior_snapshot`/`triptych_queue`, chat intent (anchored fullmatch "triptych priors"/"the review queue"/"what does history say"). Parity reference: India/REER PIT hz-12M relative = decile 1, n 20, +25.5% avg fwd, 100% hit, IC −0.41. PIT canaries in `tests/loop/test_triptych.py`. Contracts: governance v1.3, loop_schema v1.2. The old ASADO-Triptych worktree was salvaged (URL builder + workflow doc into main; xlsx-based scan archived in `docs/salvage/`) and removed.
- Triptych queue rule HARNESS VERDICT (2026-07-02): the review-queue membership rule was registered and tested as ONE hypothesis (`H_20260702_001`, family `triptych_prior`, prefix `TRIPTYCH_` in `config/family_registry.yaml` v1.1) — verdict **DEAD** (1m mean IC −0.003, NW-t −0.24, gross LS Sharpe −0.26, negative in 3 of 4 subperiods, 221 months 2008+). The test used `scripts/loop/build_triptych_signal.py`: a walk-forward reconstruction of the queue rule (loop table `triptych_signal_monthly`, variables `TRIPTYCH_QUEUE_LEAN`/`TRIPTYCH_QUEUE_N`) where each month's value uses only completed forward windows (records ≤ T−h), running PIT thresholds, a 1-month factor availability embargo, and the live queue gates verbatim; no-lookahead proven by input-truncation canary (`--truncate-input`, bit-identical overlap). CONSEQUENCE: Triptych priors stay CONTEXT/PRIOR-tier everywhere (cockpit letter, Fable packet, MCP) — the aggregate lean must NOT be treated as a return signal or added to the combiner; any future re-test (e.g. gate variants, confidence-weighted subsets) is a NEW trial charged to family `triptych_prior`. Builder is on-demand, not nightly (signal table survives in the loop DB/parquet).
- DuckDB lock guard (2026-07-03, permanent fix): `scripts/duckdb_lock_guard.py::guarded_connect()` now wraps every pipeline open of the main `asado.duckdb` (`build_daily_panels`, `predmkt_daily_job`, `build_predmkt_panel`, `setup_duckdb`, `build_normalized_panel`, `build_event_log`, `build_variable_registry` — rw AND ro sites). Background: idle Claude-Desktop analysis kernels under `~/.claude-science/` opened the DB and never closed it, killing both nightly writers on Jul 2 + Jul 3 (a read-only holder blocks writers too — DuckDB is one-writer OR many-readers). The guard parses the holder exe+PID from DuckDB's own lock error, verifies via `lsof` that the PID really holds the DB file (PID-reuse protection; DuckDB reports the RESOLVED exe, so venv symlinks can point outside the pattern dir — pattern may match exe OR live `ps` command line), auto-kills only processes matching `DEFAULT_KILLABLE_PATTERNS` (`.claude-science`; never an ASADO job, never self/parent, SIGTERM→SIGKILL, zombies treated as gone), waits with backoff up to 300s for legitimate holders (overlapping loop ATTACH / monthly rebuild), then FAILS LOUDLY naming the culprit. Env knobs: `ASADO_LOCK_GUARD_KILL=0` (wait-only), `ASADO_LOCK_GUARD_KILLABLE=a,b` (pattern override), `ASADO_LOCK_GUARD_WAIT_S`. If a NEW kind of squatter ever exhausts the budget, the error message says which process — add its pattern to `DEFAULT_KILLABLE_PATTERNS` if it's a sandbox/notebook, otherwise let it finish. E2E-verified: both nightly jobs run to completion with a live squatter holding the lock (guard kills it in ~1s).
