# End-of-Day Report — 2026-06-10

All six items from the morning list are built, tested, and verified end-to-end.
22/22 tests pass; the full loop daily job runs clean with every step OK.

---

## 1. The three paper theses — reviewed against corrected math

The overnight theses were generated by v1.0 detectors that had a calendar bug
(rolling windows counted weekend placeholder rows as days). Before judging the
theses I rebuilt the machinery (sections 3–6 below) and recomputed each entry
signal under the corrected v1.1 engine. The verdicts are recorded as
`thesis_review` events in the thesis ledger.

| Thesis | Entity | Entry claim | Recomputed (v1.1) | Verdict |
|---|---|---|---|---|
| T_20260610_001 | Indonesia long | gap_z +1.73, own −2.1 | gap_z **+2.33**, own **−2.98** | **AGREE** — signal stronger under honest measurement. Stays open. (Note: book already long EIDO 3.2%, so this duplicates a live exposure.) |
| T_20260610_002 | Hong Kong long | gap_z +2.09, own −1.7 | gap_z **+1.90**, own **−1.93** | **AGREE** — slightly softer, still well above the 1.5 bar. Stays open. |
| T_20260610_003 | Taiwan short | gap_z −2.28, own +1.33 | gap_z **−1.11**, own **+0.28** | **KILL** — the entry signal was a measurement artifact of the v1.0 window bug. Below firing threshold when computed correctly. Closed as `killed_review` with outcome=None (a kill-at-review for bad measurement is not a forecast outcome; scoring it would pollute the Brier ledger). |

That's exactly the calibration data the review was for: 1 of 3 overnight
theses did not survive correct arithmetic.

## 2. `t2_levels_daily` staleness — root cause found and fixed

**Root cause:** `build_t2_master_daily.py` winsorized level series (PX_LAST,
Currency, 10Yr Bond) against their **full-sample** median ± 5×MAD. For any
trending series this is a static ceiling: SPX froze at 5331.89 from 2025-04,
TRY froze from 2020-04 — and it was lookahead besides.

**Fix (v1.1):** trailing 252-day rolling median ± 5×rolling-MAD clip. Adapts
to trends, still kills one-off data-error spikes, no lookahead.

**Verified after rebuild:** SPX now 7334.08 (was 5331.89), TRY 46.14 and
moving daily (was frozen), Korea shows the +9% June-8 bounce — 28–29 distinct
values per series since May 1.

**Bonus find from the new D4 freshness guard:** Taiwan's 10Yr Bond series has
been genuinely dead upstream **since 2020-05-20** (not a winsorize artifact —
the raw pull is static). Philippines (last change 05-15) and Vietnam (01-26)
are also stale. These are now reported loudly as DETECTOR_DEGRADED rows every
night instead of silently producing fake z-scores.

## 3. One shared return engine (the fix beneath everything)

`t2_factors_daily.1DRet` is forward-labeled on a calendar grid with 0.0
weekend placeholders. Every v1.0 consumer rolled windows over that grid —
windows spanned different economic days per country and labels were shifted a
day into the future.

New shared helpers in `scripts/loop/loopdb.py` — `daily_country_returns()`
(backward-labeled, real trading days only) and `returns_panel()` (union
trading calendar, ≥10 countries per date). Graph features, all detectors, and
the daily harness now consume this single definition.

## 4. D9 v1.1 + D4 v1.1 (and D1 live)

- **D9 (T2 vs ETF basis):** both legs now compound over the **identical
  common date set** — the calendar can no longer manufacture a gap. Result:
  one real firing (Philippines, z=+2.2) instead of v1.0's pile of
  false positives. ETF legs that stop updating get excluded with a DEGRADED
  notice after 7 days.
- **D4 (cross-asset coherence):** unblocked by the staleness fix; FX/10Y
  level series are compressed to real observations before 5-trading-day
  windows; per-country freshness guards (7 days); structural exclusions for
  the USD trio (no FX leg) and pegs (Saudi, HK band). First real firing:
  Australia — equity −1.7z with FX strengthening +1.23z.
- **D1 (terms-of-trade impulse, archetype A1)** is **live** — see next.

## 5. D1: export-weighted ToT impulse (v0.5)

**Data:** new collector `scripts/loop/build_tot_shares.py` pulls World Bank
WDI commodity trade composition (fuel / ores&metals / food / agri raw, % of
merchandise exports and imports). These four categories map 1:1 onto Pink
Sheet aggregate indices already in the warehouse (IENERGY, IMETMIN, IFOOD,
IRAW_MATERIAL). 33/34 countries covered; **Taiwan has no WDI data** —
reported loudly, structural until Comtrade SITC-2 lands (Phase 3). Shares
pass the sanity test: Saudi net fuel +73.7%, Chile net metals +54.2%, Japan
net fuel −21.3%.

**Detector:** per-country impulse = Σ(net share × Pink Sheet 3m return),
z-scored vs trailing 36 months. Fires when |z| ≥ 1.5 AND own 21d equity z
< 0.5 AND REER 3m-change z < 0.5 (i.e. the shock has NOT been repriced).
Staleness guard: Pink Sheet data older than 60 days degrades the detector.

**First night's catch** (energy index +45% over 3m, z=+2.4, is a live shock):

| Entity | Dir | ToT z | Impulse | Read |
|---|---|---|---|---|
| Saudi Arabia | long | +2.33 | +32.3% | the energy shock, unpriced (own 21d z = 0.0) |
| U.S. / US SmallCap | long | +2.38 | +5.1% | net energy exporter, equity actually down |
| Japan | short | −2.40 | −10.5% | big energy importer, not yet repriced |
| Switzerland | short | −2.44 | −1.1% | statistically extreme but economically small — impulse magnitude is in the components for exactly this judgment |

## 6. PIT graph weights

`build_graph_features.py` v1.1 stores a monthly edge-weight snapshot
(`graph_edge_snapshots`, 2,308 edge rows for 2026-06) from Neo4j
trade/banking/holder edges, and computes features using the snapshot in force
at each historical date. First snapshot = 2026-06, so history before that
honestly uses 2026-06 weights — recorded in the table meta rather than
pretended away. From next month onward the features become genuinely
point-in-time as snapshots accumulate.

## 7. Daily harness v2 — and graph_trade_gap demoted WATCH → WEAK

v2 gives daily runs the full gate set instead of IC-only:

- **Returns:** corrected backward-labeled trading-day returns.
- **Portfolios:** overlapping-tranche construction (Jegadeesh-Titman) —
  hold_days tranches, each re-ranks on its own offset, next-day execution
  (no same-close fills). Three new execution-timing canary tests prove the
  lag is exactly one day: contemporaneous-cheat signal earns ~nothing,
  next-day-foresight signal earns hugely, two-day-foresight earns nothing.
- **Costs:** 10/25/50 bps one-way × actual turnover + 50 bps/yr borrow.
- **Deflated Sharpe:** now period-aware (252/yr for daily).
- **NW t-stat fix:** max_lag now equals the horizon in trading days; v1 used
  h//21 → lag 0 for the 5d horizon, which overstated the t-stats (7.10 → 3.05
  on the same signal family). Treat archived v1 daily t-stats accordingly.

**graph_trade_gap re-run (H_20260610_003), full gates:**

| Gate | Result |
|---|---|
| NW-t (5d primary) | 3.05 — PASS |
| Positive IC years | 73.7% — PASS |
| Net-25bps LS Sharpe | **−1.03 — FAIL** |
| Deflated Sharpe | **−0.065 — FAIL** |
| **Verdict** | **WEAK** (was WATCH on IC-only v1) |

The gross edge is real (LS Sharpe 0.57 gross, top-7 excess +5.5%/yr) but the
signal trades too fast: 7.8%/day one-way turnover devours it. Net of 10 bps
the long-only excess survives (+1.2%/yr); at 25 bps everything is gone. Also
honest: the edge was concentrated 2008–2017 (gross LS Sharpe ~1.1) and has
been flat since 2018. If this family comes back it needs a slower expression
(longer hold, rebalance bands) registered as a NEW trial — not a quiet retry.

## 8. Wiring and ordering (why this morning's run scanned stale data)

The 06:45 launchd loop job ran before the data pipeline finished (~08:40).
Fixes:

- `daily_update.py` v1.1 now runs the **full loop job as its final stage**
  (skippable with `--skip-loop`), so detectors always see the data the run
  just built.
- `loop_daily_job.py` gained the `build_tot_shares` step (keeps the existing
  table if WDI fetch fails).
- The dislocation engine is idempotent per run date, so tonight's run will
  also overwrite the overnight v1.0 rows dated 2026-06-10 with v1.1 output —
  the table self-heals without manual deletion.
- **Recommendation (needs your call, I did not touch launchd):** either
  retime `com.arjundivecha.asado-loop-daily` to after the data pipeline, or
  put `daily_update.py` itself on the schedule and drop the standalone loop
  job. As is, the 06:45 run is harmless but redundant on days the pipeline
  runs.

## 9. Verification

- `pytest tests/loop/` — **22 passed** (14 existing + 8 new daily-v2 honesty
  tests, including the three execution-timing canaries).
- `loop_daily_job.py` end-to-end — **all 7 steps OK** (news bridge, marks,
  country returns, ToT shares, graph features + snapshot, dislocations +
  brief, ledger fold).
- 52 dislocation rows written for 2026-06-09; brief rendered.

## Files touched

| File | Change |
|---|---|
| `scripts/build_t2_master_daily.py` | v1.1 rolling winsorize (staleness root cause) |
| `scripts/loop/loopdb.py` | shared `daily_country_returns` / `returns_panel` |
| `scripts/loop/build_graph_features.py` | v1.1 PIT monthly edge snapshots + corrected returns |
| `scripts/loop/build_tot_shares.py` | NEW — WDI commodity trade-share collector |
| `scripts/loop/build_dislocations.py` | v1.1 — D1 live, D4/D9 fixed, freshness guards, DEGRADED rows |
| `scripts/harness/evaluate_signal.py` | v2.0 — daily tranche portfolios, costs, deflated Sharpe, NW-lag fix |
| `scripts/loop/loop_daily_job.py` | + build_tot_shares step |
| `scripts/daily_update.py` | v1.1 — loop chained as final stage |
| `tests/loop/test_harness_daily_v2.py` | NEW — 8 honesty tests |
| `ledgers/thesis_ledger.jsonl` | 3 review events + 1 kill |

---

# Evening session addendum — 2026-06-10

Four more work streams completed after the morning report.

## 10. "8 factors" artifact removed system-wide

You flagged that the "8 live strategy factors" concept was not meaningful — it
traced to a stale static file (`T2_Optimizer_Top.xlsx`) that had leaked into
the schema as `is_optimizer_selected`. Removed everywhere:

- File deleted; `variable_meta` column dropped (index recreated);
  `build_daily_panels.py`, `build_predmkt_panel.py`, `setup_neo4j.py`,
  `asado_mcp_server.py` all cleaned (MCP now raises a clear error if a
  caller asks for `variables="optimizer"`).
- D7 crowding now scans **all 178 factors** dynamically instead of a
  hardcoded 8; herding entity renamed `FACTOR_HERDING`.
- All docs and the schema cache regenerated.

## 11. Semantic Registry Phase 2 — Tier-A semantics authored

- NEW `config/tier_a_semantics.yaml`: 242 hand-authored base-variable
  definitions (units, cross-country comparability, economic mechanism,
  concept tags) covering T2/Bloomberg, IMF, World Bank, FRED, OECD, BIS,
  macrostructure composites, prediction markets, and more.
- NEW `scripts/apply_tier_a_semantics.py` merges them into
  `config/variable_registry_seed.yaml` and propagates to `_CS`/`_TS`
  variants (plus templated ECB FX and Pink Sheet commodity entries).
- Coverage: **1,300 of 1,434 registry entries** now have real semantics
  (remainder = lower-priority GDELT). CI green; data dictionary re-rendered.

## 12. Forward event calendar (PRD Priority 5) — live

- NEW `config/forward_calendar_seed.yaml`: verified 2026 dates for FOMC,
  ECB, BOJ, BOE, RBA, Copom, RBI, BOK, Banxico; MSCI review dates (incl.
  the June 23 Vietnam classification decision); FTSE Vietnam EM
  implementation; US midterm / Brazil / Sweden / Taiwan elections.
- NEW `scripts/loop/build_forward_calendar.py` loads it into the loop DB
  with hard validation + staleness warnings; wired into `loop_daily_job.py`;
  the nightly brief now renders a "next 30 days" catalyst section.

## 13. Foreign investor flows (PRD Priority 6) — SIX markets live

Network reality check on the 7 PRD exchanges: TWSE times out (geo-block),
KRX/SET/IDX return 403, PSE needs report scraping, B3 needs endpoint
discovery. Two-source solution built:

**India — NSDL FPI direct (works from this network):**
- NEW `scripts/loop/collect_foreign_flows.py` — drives NSDL's ASP.NET
  archive (GET tokens → POST date), parses the depository-confirmed daily
  FPI table, handles holiday-date empty responses, caches every raw HTML
  response. Backfilled 2017→today: 493 weekly requests, 0 failures,
  2,147 trading days. Variables: equity net/gross buy/gross sell, debt
  net, total net (all USD mn, source=nsdl_fpi).

**Korea, Taiwan, Thailand, Philippines, Indonesia — Bloomberg (your
suggestion; Terminal came back up late evening):**
- Discovered + verified the exchange-sourced flow tickers via
  `//blp/instruments`: KPCPNTFR/KQCPNTFR (KOSPI/KOSDAQ net, KRW),
  TAFINET/TAFIBUY/TAFISELL (TWSE, TWD), THIVNET$ (SET, USD),
  VUPHBNET (PSE, USD), JASXFIBA/JASXFISA (IDX buy/sell, IDR).
- Cross-validated against India: Bloomberg FIINNET$ 09-Jun = −406.3 USD mn,
  identical to NSDL's 10-Jun reporting row (−406.29) → Bloomberg uses
  trade dates, NSDL reports T+1.
- NEW `scripts/loop/collect_foreign_flows_bbg.py` (OpusBloomberg env) —
  pulls all five markets, converts KRW/TWD/IDR to USD with same-day FX,
  $20bn/day sanity guard. **Full 2005→today backfill in 10 seconds**
  (~5,200 days per market).
- History validates against known episodes: COVID crash Feb–Mar 2020 =
  Korea −$13.5B / Taiwan −$15.0B / India −$9.2B; 2008 Korea −$36.6B;
  2022 Taiwan record −$42.1B. Checked Korea 2026 YTD (−$78.7B, looks
  extreme) — distribution is smooth, worst day −$4.9B, genuinely the
  heaviest foreign selling on record, not an artifact.
- Panel: `Data/work/loop/foreign_flows.parquet` (57.8K rows, 6 countries,
  2005→today) + loop-DB `foreign_flows_daily`; timestamped backup taken.
- Both steps wired into `loop_daily_job.py` (Bloomberg step runs first,
  parquet-only; NSDL step merges India + rebuilds the DB table). Brief
  shows last/5d/20d + 5d z per country. First night's read: **Korea 5d
  z=−2.6 (−$11.3B/5d), Taiwan z=−2.7, India z=−1.8** — the EM foreign
  flow tape is heavily one-sided right now.
- Still open: Vietnam, Saudi, Brazil (no Bloomberg exchange-flow series
  found in first search pass). Valuation/CDS pulls (Priorities 7–8) can
  now proceed since the Terminal is back.

## Evening files touched

| File | Change |
|---|---|
| `config/tier_a_semantics.yaml` | NEW — 242 Tier-A semantic definitions |
| `scripts/apply_tier_a_semantics.py` | NEW — merge + variant propagation |
| `config/forward_calendar_seed.yaml` | NEW — curated 2026 catalyst dates |
| `scripts/loop/build_forward_calendar.py` | NEW — loads calendar into loop DB |
| `scripts/loop/collect_foreign_flows.py` | NEW — NSDL India FPI flows collector |
| `scripts/loop/collect_foreign_flows_bbg.py` | NEW — Bloomberg flows: KR/TW/TH/PH/ID, 2005+ |
| `scripts/loop/build_dislocations.py` | + calendar & flows brief sections; D7 all-factor scan |
| `scripts/loop/loop_daily_job.py` | + build_forward_calendar, collect_foreign_flows steps |
| `scripts/build_daily_panels.py`, `scripts/build_predmkt_panel.py`, `scripts/setup_neo4j.py`, `scripts/asado_mcp_server.py` | 8-factor artifact removal |
